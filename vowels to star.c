#include<stdio.h>
int main()
{
	char name[20];
	int j,a,e,i,o,u;
	printf(a="Claky people often suffer from procrastination");
    scanf("%s",&a );
    if (j==a,e,i,o,u)
    printf("*", &j);
    else
    printf("a");
    return 0;
}
