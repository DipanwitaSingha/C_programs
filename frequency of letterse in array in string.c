#include<stdio.h>
#include<string.h>
int main()
{
	char[50],a, st[500];
	int i,j,n,st,a;
	int c=0;
	printf("Enter the string:");
	fgets(a,sizeof(st),stdin);
	printf("Enter the character to find the frequency:");
  for (i=0;st[i]!='\0';i++);
  {
  	printf("Frequency of %c = %d", a,c);
  	
  	c++;
    return 0;
  }

   
}
